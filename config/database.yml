default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  timeout: 5000

development:
  <<: *default
  database: online_bookstore_development

test:
  <<: *default
  database: online_bookstore_test

production:
  <<: *default
  database: online_bookstore_production
